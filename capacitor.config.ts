import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prueba1AppMovil',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
